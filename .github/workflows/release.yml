# Сборка и публикация при создании тега v*
name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      version: ${{ github.ref_name }}
    secrets: inherit

  release:
    name: Upload GitHub Release
    needs: [build]
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare release files
        run: |
          VER="${GITHUB_REF_NAME#v}"
          [[ "$VER" =~ ^[0-9] ]] || VER="1.0.0"
          echo "VER=$VER" >> $GITHUB_ENV

          cd artifacts

          if [ -f android-apk/app-release.apk ]; then
            mv android-apk/app-release.apk ../legion-android-$VER.apk
          else
            echo "APK not found!" && exit 1
          fi

          if [ -d ios-app ]; then
            mv ios-app ../legion-ios-$VER.app
          else
            echo "ios-app not found!" && exit 1
          fi

          if [ -f linux-installer/legion-linux.tar.gz ]; then
            mv linux-installer/legion-linux.tar.gz ../legion-linux-$VER.tar.gz
          else
            echo "linux-installer artifact not found!" && exit 1
          fi

          DEB=$(find linux-installer -maxdepth 1 -name 'legion-*-amd64.deb' -print -quit)
          if [ -z "$DEB" ] || [ ! -f "$DEB" ]; then
            DEB="linux-installer/legion-amd64.deb"
          fi
          if [ -n "$DEB" ] && [ -f "$DEB" ]; then
            cp "$DEB" ../legion-linux-$VER.deb
          else
            echo "linux .deb not found! Contents of linux-installer:"
            ls -la linux-installer/
            exit 1
          fi

          if [ -f windows-installer/LegionSetup.exe ]; then
            mv windows-installer/LegionSetup.exe ../legion-windows-$VER.exe
          elif [ -f LegionSetup.exe ]; then
            mv LegionSetup.exe ../legion-windows-$VER.exe
          else
            echo "windows-installer artifact not found!" && exit 1
          fi

          if [ -f macos-installer/legion-macos.dmg ]; then
            mv macos-installer/legion-macos.dmg ../legion-macos-$VER.dmg
          else
            echo "macos-installer artifact not found!" && exit 1
          fi

      - name: Upload Release to GitHub
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          files: |
            legion-android-${{ env.VER }}.apk
            legion-ios-${{ env.VER }}.app
            legion-linux-${{ env.VER }}.tar.gz
            legion-linux-${{ env.VER }}.deb
            legion-windows-${{ env.VER }}.exe
            legion-macos-${{ env.VER }}.dmg
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
